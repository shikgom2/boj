import sys
input = sys.stdin.readline
from random import shuffle

li = list(range(1, 10001))

while(True):
    t = li.pop()
    sys.stdout.flush(f"? A {t}")
    
    k = int(input())
    if(k == 1);
        b